#!/bin/sh
set -e

case "${1:-}" in
    remove|deconfigure)
        if command -v systemctl >/dev/null 2>&1; then
            systemctl disable --now jx-sentinel.service jx-sentinel-guard.service || true
        fi
        ;;
esac

exit 0

